import { createReducer } from 'reduxsauce';
import { Types } from '@Actions/individualProject';

const initialState = {};

const datasetPopupReducer = createReducer(initialState, {});

export default datasetPopupReducer;
